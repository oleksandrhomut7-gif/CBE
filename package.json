{
  "name": "stillwork",
  "version": "0.1.0",
  "description": "A focus-density task manager. One task. One moment. Your personal efficiency index.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^3.2.0",
    "@vitest/coverage-v8": "^3.2.0"
  }
}
